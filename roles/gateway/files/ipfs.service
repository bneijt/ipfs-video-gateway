[Unit]
Description=IPFS daemon
After=local-fs.target

[Service]
ExecStart=/opt/ipfs/go-ipfs/ipfs daemon
WorkingDirectory=/opt/ipfs
User=ipfs
Group=ipfs
Nice=19
Restart=always

[Install]
WantedBy=basic.target
